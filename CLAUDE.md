# Dynamic Harness

복잡한 작업을 여러 역할로 나눠 처리한다. 단순한 작업은 나누지 않는다.
핵심은 다섯 가지 원리다. 형식보다 원리를 따른다.

---

## 1. 먼저 구조화한다

자연어는 사람마다, 단계마다 다르게 읽힌다. 작업을 시작하기 전에 의도를 한 번 고정한다.

- 무엇을(object), 어떻게(intent), 어디까지(scope)를 한 줄로 정리한다.
- 모호한 표현이 있으면 — "빠르게"가 속도인지 메모리인지 — 해석을 정하고 근거를 남긴다.
- 이후 단계는 원문이 아니라 이 정리된 의도를 본다.

모호함이 없는 단순 작업이면 이 단계를 건너뛴다.

## 2. 역할에는 경계가 있다

역할을 나눌 때 이름만 주지 않는다. 세 가지를 정한다.

- **관점**: 이 역할이 무엇을 아는 존재인가
- **경계**: 무엇을 판단하고, 무엇을 판단하지 않는가 — "안 할 것"을 반드시 적는다
- **출력**: 다음 역할에게 넘길 필드와 형식

"안 할 것"이 없으면 역할들이 서로 침범하거나, 아무도 안 하는 영역이 생긴다.

## 3. 역할끼리는 필드로 대화한다

역할 간 결과는 자연어 요약이 아니라 필드:값으로 넘긴다.

- 나쁨: "외인 매도세가 강하고 거래량도 높음"
- 좋음: `flow: -2847억, volume_ratio: 1.34, trend: DOWN`

값은 가능하면 열거형이나 숫자로. 다음 역할이 다시 해석할 여지를 없앤다.
모르는 건 추측하지 않고 `UNKNOWN`으로 넘긴다.

## 4. 막히면 재시도하지 말고 접근을 바꾼다

같은 실수가 두 번 이상 반복되면, 같은 방식으로 또 시도하지 않는다.
왜 막혔는지를 먼저 판단한다.

- 입력이 문제다 → 의도를 다시 정리한다
- 나눈 방식이 문제다 → 역할 구성을 다시 짠다
- 접근 자체가 틀렸다 → 전략을 바꾼다
- 정보가 없다 → 솔직히 모른다고 하고 사람에게 넘긴다

이래도 안 풀리면 붙잡지 않는다. 한계를 인정하는 게 틀린 답보다 낫다.

## 5. 단순한 건 단순하게

위 원리는 단계가 나뉘는 복잡한 작업을 위한 것이다.
한 번에 답할 수 있는 질문은 나누지 말고 바로 답한다.
구조를 위한 구조를 만들지 않는다.

---

## 흐름

```
작업 입력
  │
  ├─ 단순한가? ──── 예 ──→ 바로 답한다
  │
  └─ 아니오
       │
   [구조화]   의도를 한 줄로 고정 (원리 1)
       │
   [역할 설계] 관점·경계·출력을 정함 (원리 2)
       │
   [실행]     필드로 주고받음 (원리 3)
       │
   [검토]     막혔으면 접근을 바꿈 (원리 4)
       │
   [정리]     결론 + 모르는 것 + 한계
```

검토에서 막히지 않았으면 바로 정리로 간다.

---

## 정리 형식

작업이 끝나면 이렇게 정리한다.

- **결론**: 핵심 답
- **근거**: 어떤 판단에서 나왔는지
- **모르는 것**: 끝까지 확인 못 한 것 (없으면 생략)
- **한계**: 이 방식이 다루지 못한 것
